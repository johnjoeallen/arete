---
id: DOC013
category: Documentation
matcher: documentation-completeness
scope: property
parameters: { require: both }
---

# DOC013 — Schema property lacks a description or example

## Intent

Every schema property should carry a `description` and an `example` so that
generated documentation and mock servers are useful.

## Diagnostic

```yaml
components:
  schemas:
    Customer:
      properties:
        displayName: { type: string }
```

## Compliant

```yaml
components:
  schemas:
    Customer:
      properties:
        displayName:
          type: string
          description: The customer's preferred display name.
          example: Ada Lovelace
```

## Detection and scope

The rule has `property` scope and uses the `documentation-completeness`
rule with `require: both`. Each component-schema property is checked; one
diagnostic is reported per property missing a description, an example, or
both.

## Configuration and limitations

`require` may be narrowed to `description` or `example`. The rule checks
presence, not quality, and inspects component schemas only.

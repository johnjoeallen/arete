# speculate
Speculate — a local-first API explorer for OpenAPI/Swagger specs. Paste a spec, get instant browsable docs with search, tabs, and pluggable custom validation — no cloud, no build step, single runnable jar.

# Starter API Policy

The starter policy assigns a single once-per-rule deduction. It will take
effect when the generic policy engine and its `resource-path` detector are
implemented.

```yaml policy
REST001: 10
```

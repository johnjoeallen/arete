---
id: REST003
category: Resource design
detector: resource-path
scope: operation
parameters: { match: rpc-style }
---

# REST003 — API uses RPC-style resource design

HTTP APIs should normally model resources rather than expose each operation as a remote procedure call.

---
id: UPDATE002
category: Update semantics
detector: operation
scope: operation
parameters: { method: PATCH }
---

# UPDATE002 — PATCH is used

PATCH represents partial modification of a resource. Its use is a policy choice rather than an inherently invalid design.

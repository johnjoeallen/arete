---
id: UPDATE003
category: Update semantics
detector: manual
scope: operation
---

# UPDATE003 — Update could be represented as a sub-resource

An independently updateable concept may sometimes be clearer when represented as a sub-resource. This is a manual review item because an OpenAPI contract cannot reliably establish the design intent.

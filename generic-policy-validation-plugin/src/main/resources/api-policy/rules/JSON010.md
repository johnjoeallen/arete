---
id: JSON010
category: JSON
detector: common-field
scope: property
parameters: { convention: default }
---

# JSON010 — Common field has an inconsistent type

Common fields should use consistent types: `id` as string, and `created` or `modified` as date-time strings.

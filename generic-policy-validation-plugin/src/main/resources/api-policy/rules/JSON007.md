---
id: JSON007
category: JSON
detector: schema
scope: property
parameters: { type: string, enum: present }
---

# JSON007 — Closed string enum is used

The API models a finite set of values using a closed string enum. This is a policy choice.

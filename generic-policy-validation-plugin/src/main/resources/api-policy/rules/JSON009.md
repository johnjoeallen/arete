---
id: JSON009
category: JSON
detector: schema
scope: property
parameters: { type: integer, enum: present }
---

# JSON009 — Numeric enum is used

The API represents a finite set of semantic values using numeric enums.
